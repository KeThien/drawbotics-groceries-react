import React, { Component } from 'react'

export class Navbar extends Component {
  render() {
    return (
      <nav style={navStyle}>
        Navbar
      </nav>
    )
  }
}

const navStyle = {
  background: "white"
}
export default Navbar
